# ── build output ──
**/bin/
**/obj/
**/[Dd]ebug/
**/[Rr]elease/
*.user

# ── IDE ──
.vs/
.idea/

# ── runtime logs written by the benches (optional — remove if you want to track history) ──
**/logs/
**/sweep_results.txt

# ── The microsoft-ui-reactor fork is a git submodule (branch perf-lab) — tracked via .gitmodules. ──
